<!-- Dolly.Desktop/Views/DatePromptWindow.xaml -->
<Window x:Class="Dolly.Desktop.Views.DatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Width="360" Height="160"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="MessageText" TextWrapping="Wrap"/>

        <DatePicker Grid.Row="2" x:Name="DatePickerControl" Height="26"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Click="Cancel_Click" Margin="0,0,6,0"/>
            <Button Content="OK" Width="80" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>